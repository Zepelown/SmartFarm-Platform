import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());  // 앱 구동 runApp -> 안에 메인 페이지 입력해주면 됨
}

class MyApp extends StatelessWidget { // 앱 메인페이지 만들어줌
  const MyApp({super.key}) : super(key: key);

  @override
  Widget build(BuildContext context) { // 여기까지 기본적인거
    return MaterialApp( // 실제 코딩
      home:
    );
  }
}
